apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "insider.fullname" . }}-test-connection"
  labels:
{{ include "insider.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "insider.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
